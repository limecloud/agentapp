---
title: 快速开始
---

# 快速开始

创建一个包含 `APP.md` 的目录，写入必需 frontmatter，然后运行 `npx agentapp-ref validate ./my-app` 和 `npx agentapp-ref project ./my-app`。随着应用增长，再加入 Skill 引用、Knowledge 模板、Tool 需求、Artifact 类型和 Eval。
