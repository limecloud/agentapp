# Knowledge Builder Workflow

Upload files, parse them through `lime.files` and `lime.tools`, structure sections, request human review, then write knowledge items into app storage and bound Agent Knowledge packs.
