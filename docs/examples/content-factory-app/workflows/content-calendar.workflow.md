# Content Calendar Workflow

Read confirmed content scenarios and bound knowledge, generate copy batches through `lime.agent`, grade outputs with evals, request human review, and persist content artifacts with evidence.
